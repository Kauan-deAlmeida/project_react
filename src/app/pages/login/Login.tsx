import { Button } from "../../shared/components/ButtonNavigacao";

export const Login = () => {

    return (
        <div>
            Login
            <Button
                texto="Página inicial"
                pagina="/pagina-inicial"
            />
        </div>
    );
}